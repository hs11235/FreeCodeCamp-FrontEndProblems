
function reverseString(str) {
  var temp = [];
  
  temp = str.split('');
  
  temp = temp.reverse();
  
  str = temp.join();
  
  str = str.replace(/,/g, '');
  
  return str;
}

reverseString("hello");
