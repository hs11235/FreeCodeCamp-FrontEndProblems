
function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  switch (bool) {
    case true:
      return true;
    case false:
      return true;
    default:
      return false;
  }
  
  return bool;
}

booWho(null);
