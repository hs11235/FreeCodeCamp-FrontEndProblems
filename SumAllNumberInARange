
function sumAll(arr) {
 
  var total = 0;
  
  if (arr[1] > arr[0]) {
    for(var i=arr[0]; i<=arr[1]; i++) {
      total += i;
    } 
  } else if (arr[0] > arr[1]) {
    for(var j=arr[1]; j<=arr[0]; j++) {
      total += j;
    }
  } 

  
  return total;
}

sumAll([1, 4]);
